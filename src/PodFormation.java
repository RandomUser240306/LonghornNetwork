/**
 * Class used to partition students into groups, called "pods"
 */
public class PodFormation {
    /**
     * PodFormation constructor
     * @param graph the graph of students
     */
    public PodFormation(StudentGraph graph) {
        // Constructor
    }
    /**
     * Partitions students into pods so that close students stay together
     * @param podSize size of pods students should be partitioned into
     */
    public void formPods(int podSize) {
        // Method signature only
    }
}
