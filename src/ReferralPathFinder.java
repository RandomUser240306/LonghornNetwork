import java.util.*;
/**
 * Application of Dijkstra's shortest path algorithm to determine shortest internship referral path
 */
public class ReferralPathFinder {
    /**
     * ReferralPathFinder constructor
     * @param graph the graph of student connections to be used for 
     */
    public ReferralPathFinder(StudentGraph graph) {
        // Constructor
    }
    /**
     * Uses Dijkstra's algorithm to find path of strongest connection between starting student and a student that interned at the target company
     * @param start starting student
     * @param targetCompany company whose interns we want to connect with
     * @return a List representing the path of students that we should go through to get a referral at the target company
     */
    public List<UniversityStudent> findReferralPath(UniversityStudent start, String targetCompany) {
        // Method signature only
        return new ArrayList<>();
    }
}
