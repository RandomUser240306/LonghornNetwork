/**
 * Implementation of Runnable used to allow multithreading in student chats
 */
public class ChatThread implements Runnable {
    /**
     * Constructor for ChatThread
     * @param sender the UniversityStudent sending the message
     * @param receiver the UniversityStudent receiving the message
     * @param message the contents of the message
     */
    public ChatThread(UniversityStudent sender, UniversityStudent receiver, String message) {
        // Constructor
    }

    /**
     * Sets up chat between sender and receiver
     */
    @Override
    public void run() {
        // Method signature only
    }
}
