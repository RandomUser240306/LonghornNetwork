/**
 * Implementation of Runnable used for multithreading friend requests
 */
public class FriendRequestThread implements Runnable {
    /**
     * Constructor for FriendRequrestThread
     * @param sender UniversityStudent sending the friend request
     * @param receiver UniversityStudent receiving the friend request
     */
    public FriendRequestThread(UniversityStudent sender, UniversityStudent receiver) {
        // Constructor
    }
    /**
     * Sets up a friend request from sender to receiver
     */
    @Override
    public void run() {
        // Method signature only
    }
}
